profiles {
  simmap {
    process.container = 'myrepo/simmap:latest'
    docker.enabled = true
  }

  barseq {
    docker.enabled = false  // or true, if/when you containerize
  }

  // This could be used in a master workflow later:
  master {
    docker.enabled = true
  }
}
